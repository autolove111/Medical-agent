# Medical-agent 完整启动教程（无 Docker 版）

## 一、前置依赖安装

### 1. Conda 环境

```powershell
# 创建环境（首次）
conda create -n medlab-langchain python=3.11 -y
conda activate medlab-langchain

# 安装依赖（首次）
cd E:\xiangmu\dachuang\python_service
pip install -r requirements.txt
# 或者用 environment.yml
conda env update -f environment.yml
```

### 2. PostgreSQL 安装（长期记忆数据库）

#### 下载安装
1. 访问 https://www.postgresql.org/download/windows/
2. 下载 Windows 安装程序（推荐 PostgreSQL 16）
3. 运行安装程序：
   - 设置密码：`medlab_password`（与 .env 配置一致）
   - 端口：`5432`（默认）
   - 勾选 "Stack Builder" 可不选

#### 初始化数据库
安装完成后，打开 **SQL Shell (psql)** 或 **pgAdmin**：

```sql
-- 创建数据库用户
CREATE USER medlab_user WITH PASSWORD 'medlab_password';

-- 创建数据库
CREATE DATABASE medlab_db OWNER medlab_user;

-- 授权
GRANT ALL PRIVILEGES ON DATABASE medlab_db TO medlab_user;
```

#### 验证连接
```powershell
psql -h localhost -U medlab_user -d medlab_db
# 输入密码 medlab_password 后能进入即成功
```

### 3. Redis 安装（短期记忆缓存）

#### 方式 A：Memurai（Windows 原生 Redis 替代品，推荐）
1. 访问 https://www.memurai.com/get-memurai
2. 下载 Developer Edition（免费）
3. 安装后自动作为 Windows 服务运行

#### 方式 B：Redis for Windows（旧版）
1. 下载：https://github.com/microsoftarchive/redis/releases
2. 解压到目录，如 `C:\Redis`
3. 运行：
```powershell
cd C:\Program Files
redis-server.exe redis.windows.conf
```

#### 方式 C：WSL2 内安装 Redis
```bash
# 在 WSL2 终端中
sudo apt update
sudo apt install redis-server
sudo service redis-server start
```

#### 验证 Redis
```powershell
redis-cli ping
# 返回 PONG 即成功
```

---

## 二、项目配置

### 1. 环境变量

编辑 `python_service/.env`：

```env
# 模型路径（根据实际路径调整）
LLM_MODEL_PATH=../models/Qwen2.5-7B-Instruct
RAG_USE_LOCAL_EMBEDDING=true
RAG_LOCAL_EMBEDDING_PATH=../models/bce-embedding-base_v1
VECTOR_DB_PATH=./harness/memory/knowledge/data/vector_db

# PostgreSQL 配置
DATABASE_URL=postgresql://medlab_user:medlab_password@localhost:5432/medlab_db

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
```

### 2. 下载模型权重（首次）

```powershell
conda activate medlab-langchain

# Qwen2.5-7B-Instruct (~14GB)
python -c "from modelscope import snapshot_download; snapshot_download('qwen/Qwen2.5-7B-Instruct', cache_dir='E:/xiangmu/dachuang/python_service/models/Qwen2.5-7B-Instruct')"

# bce-embedding-base_v1 (~1GB)
python -c "from huggingface_hub import snapshot_download; snapshot_download('maidalun1020/bce-embedding-base_v1', local_dir='E:/xiangmu/dachuang/python_service/models/bce-embedding-base_v1')"
```

---

## 三、启动服务（5 个终端）

### 终端 1：PostgreSQL（如果是安装为服务则自动启动）

```powershell
# 如果安装时选择了作为服务，已自动启动
# 手动启动：
net start postgresql-x64-16
```

### 终端 2：Redis

```powershell
# Memurai 安装后自动作为服务运行
# 或手动启动：
redis-server

# WSL2 方式：
# wsl sudo service redis-server start
```

### 终端 3：Python 后端

```powershell
conda activate medlab-langchain
cd E:\xiangmu\dachuang\python_service
python server.py
# → http://localhost:8000 (Swagger: /docs)
```

### 终端 4：PaddleOCR 服务

```powershell
conda activate medlab-langchain
cd E:\xiangmu\dachuang\ai-services-python\ocr_service
python paddle_server.py
# → http://localhost:8001 (首次启动自动下载 PP-OCRv4 模型 ~80MB)
```

### 终端 5：前端

```powershell
cd E:\xiangmu\dachuang\frontend-vue
npm install    # 首次
npm run dev
# → http://localhost:8888
```

---

## 四、验证连接

### 测试 Redis + PostgreSQL

在 `python_service` 目录下运行：

```powershell
conda activate medlab-langchain
cd E:\xiangmu\dachuang\python_service
python -c "
import redis, sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
from sqlalchemy import create_engine, text

# 测试 Redis
print('=== 测试 Redis ===')
try:
    r = redis.Redis(host='localhost', port=6379, decode_responses=True, socket_connect_timeout=5)
    r.ping()
    r.set('test:memory', 'redis_ok')
    print(f'[OK] Redis 读写成功: {r.get(\"test:memory\")}')
    r.delete('test:memory')
except Exception as e:
    print(f'[FAIL] Redis: {e}')

# 测试 PostgreSQL
print()
print('=== 测试 PostgreSQL ===')
try:
    engine = create_engine('postgresql://medlab_user:medlab_password@localhost:5432/medlab_db', connect_args={'connect_timeout': 5})
    with engine.connect() as conn:
        conn.execute(text('SELECT 1'))
        tables = conn.execute(text(\"\"\"SELECT table_name FROM information_schema.tables WHERE table_schema='public'\"\"\")).fetchall()
        print(f'[OK] PostgreSQL 连接成功, 已有 {len(tables)} 张表')
except Exception as e:
    print(f'[FAIL] PostgreSQL: {e}')
"
```

---

## 五、启动顺序总结

```
1. PostgreSQL 服务（如果是 Windows 服务则开机自启）
2. Redis 服务（如果是 Memurai 则开机自启）
3. python server.py          # Python 后端
4. python paddle_server.py   # OCR 服务
5. npm run dev               # 前端
```

---

## 六、常见问题

### Redis 连接失败 (10061)
- 确认 Redis 服务已启动
- Windows 防火墙是否放行 6379 端口

### PostgreSQL 连接失败
- 确认 PostgreSQL 服务已启动：`net start postgresql-x64-16`
- 确认用户/密码/数据库名与 .env 一致
- 检查 `pg_hba.conf` 是否允许 localhost 连接

### 模型加载慢
- 首次加载需要下载模型，请耐心等待
- 4-bit 量化需要 GPU（≥6GB 显存）
- CPU 模式推理较慢，但可运行
