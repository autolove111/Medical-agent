# Medical-agent 完整启动教程

完整环境、启动、验证、服务器部署说明已合并到：

- [环境配置与部署说明.md](环境配置与部署说明.md)

历史说明中的 `E:\xiangmu\dachuang`、固定环境名 `medlab-langchain`、PostgreSQL/Redis 强依赖等内容不再作为当前项目的唯一标准。当前项目支持：

- 本地单环境：例如 `medagent`
- 本地双环境：`medlab-langchain` + `medlab-ocr`
- SQLite 本地降级
- Redis 可选增强
- PaddleOCR 独立服务

