

# 第二阶段：过渡自研（真正的自研起点，最核心、最详细）
## 核心原则
**不一次性删框架，逐个模块手写替换，逐步解耦**
**完全借鉴 Hermes 现代Harness设计思想，但不引用任何Hermes代码**

## 自研顺序（从最简单→最难，大创最优路线）
### 第一步：自研【统一模型调用层】（第一个自研模块，起步点）
#### 原来依赖
LangChain `ChatOpenAI/ChatQwen` 模型类，所有推理全靠框架封装

#### 你的自研动作（详细）
1. **新建独立模块 `llm_adapter.py`**
2. 完全手写原生 `requests` 调用，**彻底抛弃LangChain模型类**
3. 自研统一入参、出参结构体，屏蔽：OpenAI、通义、本地模型、私有化模型差异
4. 自研：超时重试、异常捕获、自动补全JSON、Token计数、流式输出
5. 适配 Hermes 多模型分流思想：
   - 推理模型（对话、思考）
   - 工具调用模型（函数解析）
   - 摘要模型（记忆压缩）

#### 效果
**你的项目模型调用彻底脱离LangChain**，这是**自研的第一个里程碑**

### 第二步：自研【Agent状态管理层】（替换LangGraph核心）
#### 原来依赖
`TypedDict/GraphState` 框架托管状态

#### 你的自研动作（详细）
1. 新建纯手写 `AgentState` 类
2. 自主定义**全量状态字段**（不再受框架限制）
```python
class AgentState:
    # 基础对话
    messages: list
    # 自研新增：结构化思考（借鉴Hermes）
    reasoning_process: str  
    # 工具执行记录
    tool_history: list
    # 任务规划队列（Hermes核心）
    task_queue: list
    # 长期记忆引用
    memory_ref: str
    # 执行状态标记
    is_finished: bool
```
3. 自研状态**更新、重置、快照、回滚**功能
4. 彻底删除所有 LangGraph State 代码

#### 效果
**你的状态流转完全自研，不再被LangGraph状态机束缚**

### 第三步：自研【核心Agent调度循环】（Harness灵魂）
#### 原来依赖
LangGraph `add_node/add_edge/compile/stream` 自动循环

#### 你的自研动作（详细）
1. **手写原生while循环**，替代框架自动调度
2. 复刻工业级Harness标准流程（对标Hermes）：
   - 1.加载当前状态 & 历史记忆
   - 2.大模型自主思考推理
   - 3.判断是否需要调用工具
   - 4.执行工具/任务
   - 5.更新全局状态
   - 6.循环直至任务结束
3. 自主实现**条件分支、循环终止逻辑**，不再配置图结构

#### 核心区别
- LangGraph：**你手动画流程图，模型跟着你的流程走**
- 自研+Hermes思想：**模型自主判断流程，系统动态调度**

### 第四步：自研【工具调用解析引擎】
#### 原来依赖
LangChain `tool_parser、Pydantic输出解析`

#### 你的自研动作
1. 手写**正则+JSON容错解析器**
2. 自研工具注册中心：统一管理所有自定义工具
3. 自主校验工具参数、权限、执行前后置校验
4. 实现工具失败重试、降级、日志记录

### 第五步：自研【分层记忆系统】（对标Hermes最大创新点）
#### 原来依赖
LangChain 单层简单内存

#### 你的自研动作（借鉴Hermes核心）
1. 自研三层记忆架构：
   - 瞬时记忆：当前对话上下文
   - 短期记忆：会话内任务记录
   - 长期记忆：向量化持久化沉淀、技能固化
2. 自研**自动记忆摘要、冗余清洗、重要度打分**
3. 实现 Hermes 特色：**任务完成后自动沉淀复用技能**

---

# 第三阶段：完全自研成熟期（彻底剥离所有框架）
当你完成上面5个模块自研后：
1. **删除所有 langchain、langgraph 导入**
2. 废弃所有框架节点、图编译、原生工具链
3. 你的系统：
   - 模型调用自研
   - 状态管理自研
   - 调度循环自研
   - 工具引擎自研
   - 记忆系统自研

**真正实现：0开源框架依赖，完全自主研发Harness架构**

---

# 专门解答你之前的问题：模型调用到底怎么办？
## 完整过渡方案（写进论文完美）
1. **初期**：复用LangChain模型调用组件，快速验证业务功能，保证项目快速落地
2. **中期（自研关键）**：抽离独立模型适配层，自研统一调用协议，屏蔽多模型差异，移除所有框架模型实例，实现模型调用解耦
3. **后期（完全自研）**：基于原生HTTP请求自主封装全套推理逻辑，自主实现上下文拼接、参数校验、输出解析、异常调度，搭建完全自主可控的模型交互体系

---

# 最终版·超详细老师建议（直接复制进系统，满分）
建议项目采用**渐进式自主研发架构**迭代开发，循序渐进完成从开源框架依赖到自研智能体Harness的升级优化：项目初期依托LangChain、LangGraph开源框架快速搭建智能体基础业务流程与模型调用链路，保障项目快速落地验证；开发中期逐步解耦框架依赖，**从统一模型调用适配层、自主状态管理、核心Agent调度循环、结构化工具解析、分层记忆五大核心模块起步完成自研重构**，同时深度参考Hermes主流智能体Harness的自进化、任务自主规划与技能沉淀设计思想，优化系统决策能力与迭代性能；项目后期彻底剥离所有第三方开源框架依赖，完善全链路自主调度、状态回滚、记忆迭代与异常容错机制，最终实现整套智能体运行架构的完全自主研发，显著提升系统的独立性、可控性与创新性。

---

# 大创专属·细化创新点（答辩必用）
1. 采用**渐进式自研方案**，解决传统智能体高度依赖开源框架、耦合度高、可定制性差的问题
2. 对标Hermes新一代Harness架构，自研**分层记忆+自主任务规划**机制，区别于传统固定流程的LangGraph智能体
3. 自研**通用模型适配中间层**，实现多模型统一调度，彻底解除框架模型调用绑定问题
4. 手写原生Agent调度内核，摆脱开源框架黑盒调度，实现智能体全流程可控可追溯

需要我把这套流程整理成**标准项目技术路线图（三段式图文描述）**，直接粘贴进结题报告吗？

你的项目根目录/
├── 📁 harness/          # 【第一层：自研通用框架层】✅ 纯自研，无业务代码
│   ├── 📁 llm_core/     # 模型加载、创建、初始化
│   ├── 📁 llm_adapter/  # 模型调用适配器
│   ├── 📁 state/        # 自研状态管理
│   ├── 📁 scheduler/    # 自研Agent调度循环
│   ├── 📁 tools/        # 通用工具引擎
│   ├── 📁 memory/       # 分层记忆系统
│   └── __init__.py
│
├── 📁 app/              # 【第二层：项目业务层】✅ 你的大创具体业务代码
│   ├── 📁 business/     # 业务逻辑（你的项目专属功能）
│   ├── 📁 prompts/      # 业务提示词
│   ├── 📁 services/     # 业务接口/服务
│   └── main.py          # 项目启动入口
│
├── configs/             # 全局配置
├── requirements.txt
└── README.md