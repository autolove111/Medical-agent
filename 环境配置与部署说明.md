# Medical-agent 环境配置与部署说明

本文是当前项目唯一推荐维护的环境说明文档。旧的 `启动教程.md`、`完整启动教程.md`、`显存与conda服务.md` 只保留跳转入口，避免路径、环境名和依赖版本互相冲突。

## 1. 当前项目形态

当前仓库目录是 `Medical-agent`，不是另一个历史目录 `MedicalAgent`。本项目采用三服务本地开发结构：

| 服务 | 目录 | 默认端口 | 作用 |
| --- | --- | --- | --- |
| Python 后端 | `python_service/` | `8000` | FastAPI、登录、聊天、报告上传、RAG、Agent、持久化 |
| OCR 服务 | `ai-services-python/ocr_service/` | `8001` | PaddleOCR 本地识别化验单图片 |
| Vue 前端 | `frontend-vue/` | `8888` | 上传、聊天、指标面板、来源展示 |

目前主链路已经按无 Docker 本地开发方式跑通：

```text
Vue 前端 (:8888)
  -> Python FastAPI (:8000)
      -> PaddleOCR (:8001)
      -> SQLite/PostgreSQL
      -> Redis 可选
      -> 本地 HuggingFace LLM/RAG 模型
```

## 2. 分支与环境名

`zly_3` 分支里的默认环境名主要沿用：

- 主后端：`medlab-langchain`
- OCR：`medlab-ocr`

你本地使用的 `medagent` 也是可以的。**conda 环境名只是本机标签，不是代码协议**。只要依赖一致，叫 `medagent`、`medlab-langchain` 或其他名字都可以。

建议团队统一写法：

- 文档示例用 `<ENV_NAME>` 表示本地环境名。
- 单环境开发可以用 `medagent`。
- 拆环境开发可用 `medlab-langchain` + `medlab-ocr`。
- 不要在代码里硬编码某个 conda 环境名。

## 3. 推荐本地环境

### 3.1 最省事：单 conda 环境

适合个人开发和调试。你当前就是这种方式。

```powershell
conda create -n medagent python=3.11 -y
conda activate medagent

cd E:\开源项目\Medical-agent\python_service
conda env update -n medagent -f environment.yml

cd E:\开源项目\Medical-agent\ai-services-python\ocr_service
pip install -r requirements.txt

cd E:\开源项目\Medical-agent\frontend-vue
npm install
```

说明：

- `python_service/environment.yml` 覆盖 FastAPI、Transformers、FAISS、RAG、数据库等主后端依赖。
- `ocr_service/requirements.txt` 覆盖 PaddleOCR 依赖。
- 单环境的优点是启动简单；缺点是依赖更重。

### 3.2 更干净：双 conda 环境

适合多人协作或后续部署拆服务。

```powershell
cd E:\开源项目\Medical-agent\python_service
conda env create -f environment.yml

cd E:\开源项目\Medical-agent\ai-services-python\ocr_service
conda env create -f environment.yml

cd E:\开源项目\Medical-agent\frontend-vue
npm install
```

启动时分别激活：

```powershell
conda activate medlab-langchain
```

```powershell
conda activate medlab-ocr
```

## 4. 环境变量

### 4.1 当前真正生效的配置文件

主后端启动时通过 `python_service/server.py` 加载：

```text
python_service/.env
```

根目录 `.env` 目前保留为历史/全局兼容配置，不是主后端的主要读取入口。修改后端 OCR、模型、RAG、数据库配置时，优先改 `python_service/.env`。

### 4.2 推荐的 `python_service/.env`

本地最小可用配置：

```env
RAG_USE_LOCAL_EMBEDDING=true

# 主模型。路径相对 python_service/ 解析。
LLM_MODEL_PATH=./models/Qwen2.5-7B-Instruct

# RAG 嵌入模型。路径相对 python_service/ 解析。
RAG_LOCAL_EMBEDDING_PATH=./models/bce-embedding-base_v1
VECTOR_DB_PATH=./harness/memory/knowledge/data/vector_db

# 本地可以先写 PostgreSQL；如果未安装 psycopg2 或连接不可用，当前代码会回退 SQLite。
DATABASE_URL=postgresql://medlab_user:medlab_password@localhost:5432/medlab_db

# Redis 是短期记忆和 RAG cache 的增强项；未启动时相关缓存会降级。
REDIS_HOST=localhost
REDIS_PORT=6379

# OCR 服务
OCR_SERVICE_URL=http://localhost:8001
OCR_SERVICE_TIMEOUT=180
OCR_ALLOW_MOCK_FALLBACK=false
```

### 4.3 路径规则

`LLM_MODEL_PATH` 和 `RAG_LOCAL_EMBEDDING_PATH` 使用相对路径时，相对的是 `python_service/` 目录。

例如：

```text
python_service/models/Qwen2.5-7B-Instruct
python_service/models/bce-embedding-base_v1
```

对应配置：

```env
LLM_MODEL_PATH=./models/Qwen2.5-7B-Instruct
RAG_LOCAL_EMBEDDING_PATH=./models/bce-embedding-base_v1
```

## 5. 模型配置

### 5.1 本地小资源机器

本地没有足够显存时，建议先保证 OCR、上传、报告结构化、前端链路跑通。LLM 首次调用才会懒加载，不影响服务启动。

可选策略：

- 使用 3B 模型替代 7B。
- 暂时只测报告上传和指标解析，不触发聊天模型。
- CPU 可运行但会很慢。

示例：

```env
LLM_MODEL_PATH=./models/Qwen2.5-3B-Instruct
```

### 5.2 本地 7B 模型

当前代码的主模型加载器是 HuggingFace 本地加载，支持 CUDA + bitsandbytes 4-bit 量化。

推荐：

- NVIDIA GPU，建议 8GB+ 显存。
- Python 3.11。
- `transformers`、`accelerate`、`bitsandbytes` 已由 `python_service/environment.yml` 管理。

下载示例：

```powershell
cd E:\开源项目\Medical-agent
conda activate medagent

python -c "from modelscope import snapshot_download; snapshot_download('qwen/Qwen2.5-7B-Instruct', cache_dir='python_service/models/Qwen2.5-7B-Instruct')"
python -c "from huggingface_hub import snapshot_download; snapshot_download('maidalun1020/bce-embedding-base_v1', local_dir='python_service/models/bce-embedding-base_v1')"
```

### 5.3 服务器资源到位后

当前代码直接本地加载 HuggingFace 模型。服务器上最稳的第一步是：

1. 在服务器部署同一套 `python_service`。
2. 把模型权重放到服务器磁盘。
3. 配置 `LLM_MODEL_PATH=/data/models/Qwen2.5-7B-Instruct`。
4. 用 GPU 跑 4-bit 或 fp16。

如果后续希望使用 vLLM、TGI、Ollama 或 OpenAI-compatible 推理服务，建议新增一个 `RemoteChatModel`/LLM proxy 适配层，让业务后端只调用远程推理 API。当前 `Medical-agent` 代码还不是这种远程 LLM 架构，不建议在没有适配层时只改 `.env` 期望它自动连 vLLM。

## 6. 数据库与 Redis

### 6.1 本地开发

本地可以不强制安装 PostgreSQL。当前 `app/persistence/database.py` 支持：

- `DATABASE_URL` 为 PostgreSQL 且依赖可用时使用 PostgreSQL。
- PostgreSQL 依赖缺失时回退 SQLite。
- 未配置数据库时默认 SQLite：`python_service/data/medagent.db`。

Redis 也是可选增强项：

- Redis 可用：短期记忆、对话缓冲和 RAG cache 可跨进程持久化。
- Redis 不可用：会记录 warning，核心上传和 OCR 不应被阻断。

### 6.2 服务器部署

服务器建议固定使用：

- PostgreSQL：保存用户、报告、对话、长期记忆。
- Redis：保存短期记忆、缓存和会话状态。
- 独立磁盘卷：保存上传文件、模型权重、向量库。

推荐路径示例：

```text
/data/medical-agent/uploads
/data/medical-agent/models
/data/medical-agent/vector_db
/data/medical-agent/postgres
/data/medical-agent/redis
```

## 7. 启动流程

### 7.1 三终端本地启动

终端 1：主后端

```powershell
conda activate medagent
cd E:\开源项目\Medical-agent\python_service
python server.py
```

终端 2：OCR

```powershell
conda activate medagent
cd E:\开源项目\Medical-agent\ai-services-python\ocr_service
python paddle_server.py
```

终端 3：前端

```powershell
cd E:\开源项目\Medical-agent\frontend-vue
npm run dev
```

访问：

```text
前端: http://localhost:8888
后端 Swagger: http://localhost:8000/docs
OCR health: http://localhost:8001/api/v1/health
```

### 7.2 启动顺序

推荐顺序：

1. OCR 服务。
2. Python 后端。
3. Vue 前端。
4. 首次上传前可访问 `/api/v1/ready` 预热 PaddleOCR。

```powershell
Invoke-RestMethod http://localhost:8001/api/v1/health
Invoke-RestMethod http://localhost:8001/api/v1/ready
Invoke-RestMethod http://localhost:8000/api/v1/health
```

## 8. OCR 验证

直测 OCR：

```powershell
$body = @{
  path = "E:\Temp\medagent_ocr\sample.jpg"
  force_recheck = $true
} | ConvertTo-Json

Invoke-RestMethod http://localhost:8001/api/v1/analyze-vision `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body ([System.Text.Encoding]::UTF8.GetBytes($body))
```

直测主后端上传：

```powershell
curl.exe -F "file=@E:\Temp\medagent_ocr\sample.jpg" `
  -F "user_id=default" `
  -F "age=0" `
  -F "gender=" `
  http://localhost:8000/api/report/upload
```

注意：

- Windows 中文路径可能在 PowerShell 请求体中被错误编码。主后端和 OCR 服务已经加入 ASCII 临时路径保护，但手动测试仍建议用 `E:\Temp\medagent_ocr\sample.jpg` 这样的 ASCII 路径。
- OCR 响应中的 `mapped_count` 是结构化指标数量，不是原始文本块数量。

## 9. 常见问题

### 9.1 OCR health 正常，但上传失败

先看两层：

```powershell
Invoke-RestMethod http://localhost:8001/api/v1/ready
Invoke-RestMethod http://localhost:8000/api/v1/health
```

如果 OCR 直连正常但主后端失败，查看主后端日志中 `api.routes.report` 和 `app.business.report_pipeline` 的异常。

### 9.2 返回内置 Mock 数据

当前默认已关闭 Mock：

```env
OCR_ALLOW_MOCK_FALLBACK=false
```

如果打开为 `true`，OCR 服务失败时会返回演示数据。正式开发和验收时应保持 `false`。

### 9.3 端口被占用

查看端口：

```powershell
Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Select-Object LocalAddress,LocalPort,State,OwningProcess
Get-NetTCPConnection -LocalPort 8001 -ErrorAction SilentlyContinue | Select-Object LocalAddress,LocalPort,State,OwningProcess
Get-NetTCPConnection -LocalPort 8888 -ErrorAction SilentlyContinue | Select-Object LocalAddress,LocalPort,State,OwningProcess
```

结束占用进程：

```powershell
Get-NetTCPConnection -LocalPort 8001 -ErrorAction SilentlyContinue |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force }
```

### 9.4 显存不足

查看显存：

```powershell
nvidia-smi
nvidia-smi --query-compute-apps=pid,process_name,used_gpu_memory --format=csv
```

建议：

- 本地优先验证 OCR 和上传链路。
- LLM 用 3B 或 CPU 慢速模式。
- 服务器上再切 7B/更大模型。

## 10. Docker 说明

当前 `Medical-agent` 仓库里只有 OCR 服务的 Dockerfile：

```text
ai-services-python/ocr_service/Dockerfile
```

它用于容器化 PaddleOCR，不代表整个项目已经完整 Docker Compose 化。完整 Docker Compose 方案属于另一个历史目录或旧架构，不应和本项目当前三终端启动方式混用。

后续如果要为当前 `Medical-agent` 做服务器 Docker 部署，建议新建当前项目自己的 compose：

```text
frontend
python_service
ocr_service
postgres
redis
model_service 或本地 GPU 模型挂载
```

在此之前，当前项目推荐使用本文件的 Conda + 三终端启动流程。

