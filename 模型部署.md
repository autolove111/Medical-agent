好的，下面一步一步教你如何将 vLLM CPU 版本集成到你的项目中，实现在自己电脑上无 GPU 也能运行，并保持最小改动。

---

### 第 1 步：下载一个轻量模型（CPU 演示用）

为了在你的电脑上快速启动和演示，我们下载 `Qwen2.5-1.5B-Instruct` 模型。如果你已经有 `Qwen2.5-7B-Instruct`，也可以继续用，但 CPU 推理会很慢且占用大量内存。

打开 **PowerShell**，在项目根目录执行：

```powershell
# 确保安装 huggingface_hub
pip install huggingface_hub

# 下载小模型到 models_hub 文件夹
huggingface-cli download Qwen/Qwen2.5-1.5B-Instruct --local-dir models_hub/Qwen2.5-1.5B-Instruct --local-dir-use-symlinks False
```

下载完成后，你可以检查一下文件是否齐全：

```powershell
ls .\models_hub\Qwen2.5-1.5B-Instruct
```

应该看到 `config.json`, `tokenizer.json`, `model.safetensors` 等文件。

---

### 第 2 步：修改 `docker-compose.yml`，加入 vLLM CPU 服务

用任意文本编辑器打开 `infrastructure/docker-compose.yml` 文件。

在 `services:` 部分**添加**一个服务定义（可以放在文件末尾）。如果你之前已经添加过 `vllm-7b`，请将其替换为以下 CPU 版本：

```yaml
  vllm-7b:
    image: vllm/vllm-openai:cpu
    container_name: medlab-vllm-7b
    command: >
      --model /models/Qwen2.5-1.5B-Instruct
      --served-model-name qwen2.5-7b
      --device cpu
      --dtype float32
      --max-model-len 2048
    ports:
      - "8100:8000"
    volumes:
      - ../models_hub:/models:ro
    environment:
      - HF_HOME=/models
    restart: unless-stopped
```

**解释**：
- `image: vllm/vllm-openai:cpu`：使用官方 CPU 专用镜像
- `--model /models/Qwen2.5-1.5B-Instruct`：容器内模型路径（对应本地 `models_hub` 文件夹）
- `--device cpu`：强制使用 CPU 推理
- `--dtype float32`：CPU 推理不支持半精度，必须用 float32
- `--max-model-len 2048`：减小上下文长度，节省内存
- `../models_hub:/models:ro`：将你的本地模型目录只读挂载进容器
- `ports: "8100:8000"`：将宿主机的 8100 端口映射到容器内 8000 端口，避免冲突

> 注意：视觉模型服务（`vllm-vlm`）暂时可以注释掉或删除，CPU 版本暂不支持多模态。

---

### 第 3 步：启动 vLLM CPU 服务

回到 PowerShell，确保在项目根目录，然后执行：

```powershell
docker compose -f infrastructure/docker-compose.yml --env-file .env up -d vllm-7b
```

Docker 会自动拉取 `vllm/vllm-openai:cpu` 镜像（约 6GB），然后启动服务。

等待 1-2 分钟，查看日志确认模型加载成功：

```powershell
docker logs medlab-vllm-7b -f
```

当你看到类似下面的输出时，说明服务已就绪：

```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

按 `Ctrl+C` 退出日志跟踪。

---

### 第 4 步：测试 API 调用

在宿主机 PowerShell 中，用 `curl` 测试（确保端口 8100 未被占用）：

```powershell
curl http://localhost:8100/v1/chat/completions `
  -H "Content-Type: application/json" `
  -d '{\"model\": \"qwen2.5-7b\", \"messages\": [{\"role\": \"user\", \"content\": \"你好，请用一句话介绍你自己\"}]}'
```

如果返回类似下面的 JSON（内容可能略有不同），说明调用成功：

```json
{"id":"cmpl-...","object":"chat.completion","choices":[{"message":{"content":"我是通义千问，阿里云研发的超大规模语言模型。","role":"assistant"}}],...}
```

> CPU 推理可能较慢（需要 20-60 秒），这是正常现象。

---

### 第 5 步：确保 LangChain 应用也能调用

你的 `langchain` 服务目前在另一个容器中，需要确认它能够通过内部网络访问 `vllm-7b`。

在你的 `langchain_service` 代码中，使用 `ChatOpenAI` 并设置 `openai_api_base` 为 `http://vllm-7b:8000/v1`。例如：

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="qwen2.5-7b",
    openai_api_base="http://vllm-7b:8000/v1",
    openai_api_key="not-needed"
)
```

如果还没有修改代码，可以先在容器外测试，保证 vLLM 本身工作正常即可。

---

### 第 6 步：向老师演示

当老师来看时，你可以这样操作：

1. **展示整个项目文件夹**，说明模型全部在 `models_hub` 中，无需联网。
2. **打开 PowerShell，运行**：
   ```powershell
   docker compose -f infrastructure/docker-compose.yml up -d vllm-7b
   ```
3. **访问 `http://localhost:8100/v1/chat/completions`**，用 Postman 或 curl 发送请求，展示返回结果。
4. 强调 **“迁移到 GPU 服务器只需将 `image` 改回 `vllm/vllm-openai:latest`，去掉 `--device cpu`，模型换成对应的大模型，即可获得高性能推理”**。

---

### 第 7 步：如何迁移到 GPU 服务器

当项目拷贝到实验室 GPU 服务器后，只需编辑 `infrastructure/docker-compose.yml`，将 `vllm-7b` 服务改为：

```yaml
  vllm-7b:
    image: vllm/vllm-openai:latest
    container_name: medlab-vllm-7b
    command: >
      --model /models/Qwen2.5-7B-Instruct
      --served-model-name qwen2.5-7b
      --dtype auto
      --gpu-memory-utilization 0.90
    ports:
      - "8100:8000"
    volumes:
      - ../models_hub:/models:ro
    environment:
      - HF_HOME=/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
```

然后重新启动即可，其他服务无需改动。

---

### 完成！

现在你已经有一个完全离线的 vLLM CPU 演示环境，可以向老师清晰地展示“打包即用”的能力。如果过程中遇到任何错误，把日志发给我，我帮你分析。