下面给你一套可直接在 PowerShell 里执行的完整命令，范围只针对你这个 `langchain_service` 的 `8001` 端口，不会误杀系统上“所有监听”。


你现在直接重新启动一次服务就行：

```powershell
Get-NetTCPConnection -LocalPort 8001 -ErrorAction SilentlyContinue |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force }

Get-CimInstance Win32_Process |
  Where-Object { $_.CommandLine -like '*uvicorn main:app*8001*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
---------------------------------------------------------------
Set-Location E:\xiangmu\dachuang\langchain_service

$env:LLM_MODEL_PATH="E:\xiangmu\dachuang\models\Qwen2.5-7B-Instruct"
$env:RAG_LOCAL_EMBEDDING_PATH="E:\xiangmu\dachuang\models\bce-embedding-base_v1"
$env:RAG_EMBEDDING_DEVICE="cpu"
$env:VECTOR_DB_PATH="E:\xiangmu\dachuang\langchain_service\knowledge\vector_db"
$env:REDIS_HOST="127.0.0.1"
$env:LLM_USE_4BIT="true"
$env:LLM_4BIT_QUANT_TYPE="nf4"
$env:LLM_4BIT_USE_DOUBLE_QUANT="true"

$stdout="E:\xiangmu\dachuang\logs\langchain-service.out.log"
$stderr="E:\xiangmu\dachuang\logs\langchain-service.err.log"

Start-Process `
  -FilePath "E:\apps\Miniconda\envs\medlab-langchain\python.exe" `
  -ArgumentList "-m","uvicorn","main:app","--host","127.0.0.1","--port","8001" `
  -WorkingDirectory "E:\xiangmu\dachuang\langchain_service" `
  -WindowStyle Hidden `
  -RedirectStandardOutput $stdout `
  -RedirectStandardError $stderr

Start-Sleep -Seconds 8
Invoke-RestMethod "http://127.0.0.1:8001/health"
```

如果这次还不通，把这两条结果贴我：
```powershell
Get-Content E:\xiangmu\dachuang\logs\langchain-service.err.log -Tail 80
Get-Content E:\xiangmu\dachuang\logs\langchain-service.out.log -Tail 40
```

我继续帮你顺着修。
```powershell
$body = @{
    query = "请说明一下肌酐130对于不同性别和年龄的人是否会有不同的意义"
    user_id = "test-user-1"
    agent_type = "nephrology"
} | ConvertTo-Json -Depth 6

$result = Invoke-RestMethod `
  -Uri "http://127.0.0.1:8001/api/v1/agent/chat" `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body $body

Write-Host ""
Write-Host "===== 回答内容 =====" -ForegroundColor Cyan
$result.content

Write-Host ""
Write-Host "===== RAG 命中情况 =====" -ForegroundColor Cyan
if ($null -ne $result.sources -and $result.sources.Count -gt 0) {
    Write-Host ("命中知识库文档数: " + $result.sources.Count) -ForegroundColor Green
} else {
    Write-Host "未命中知识库文档" -ForegroundColor Yellow
}

Write-Host ""
Write-Host "===== RAG 来源 =====" -ForegroundColor Cyan
if ($null -ne $result.sources -and $result.sources.Count -gt 0) {
    $index = 1
    foreach ($source in $result.sources) {
        Write-Host ("[来源 " + $index + "]") -ForegroundColor Green
        if ($source.metadata) {
            $source.metadata | ConvertTo-Json -Depth 6
        } else {
            Write-Host "metadata: null"
        }
        Write-Host "content preview:"
        $source.content
        Write-Host ""
        $index++
    }
} else {
    Write-Host "无来源"
}

Write-Host ""
Write-Host "===== 元数据 =====" -ForegroundColor Cyan
$result.metadata | ConvertTo-Json -Depth 6

Write-Host ""
Write-Host "===== 完整返回(JSON) =====" -ForegroundColor Cyan
$result | ConvertTo-Json -Depth 8
```
---

查看日志命令：

```powershell
Get-Content E:\xiangmu\dachuang\logs\langchain-service.err.log -Tail 100
Get-Content E:\xiangmu\dachuang\logs\langchain-service.out.log -Tail 100
```


释放服务命令：

```powershell
# 停掉 8001 监听
Get-NetTCPConnection -LocalPort 8001 -ErrorAction SilentlyContinue |
  Select-Object -ExpandProperty OwningProcess -Unique |
  ForEach-Object { Stop-Process -Id $_ -Force }

# 再补一遍按命令行特征清理
Get-CimInstance Win32_Process |
  Where-Object { $_.CommandLine -like '*uvicorn main:app*8001*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```


如果你想要，我也可以直接给你整理成一个一键执行的 `start-test-stop.ps1` 脚本。